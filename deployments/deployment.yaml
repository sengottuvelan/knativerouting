apiVersion: serving.knative.dev/v1
kind: Configuration
metadata:
  name: blue-green-demo
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: docker.io/sengottuvelan/knative:latest
          env:
            - name: T_VERSION
              value: "blue"
